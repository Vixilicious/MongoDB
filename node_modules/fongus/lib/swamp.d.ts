import type { Collection, Document } from 'mongodb';
import type { Fields } from './fields';
import type { Model } from 'mongoose';
interface Params {
    path: Collection<Document> | Model<{
        [key: string]: any;
    }>;
    fields: Fields;
    count: number;
}
export declare function swamp({ path, fields, count }: Params): Promise<any[]>;
export {};
//# sourceMappingURL=swamp.d.ts.map